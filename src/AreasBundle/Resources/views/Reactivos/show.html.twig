{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Reactivos</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Reactivosmaximos</th>
                <td>{{ entity.reactivosMaximos }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('reactivos') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('reactivos_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
