{% extends '::base.html.twig' %}

{% block body -%}
    <h1>SubArea creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('subarea') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
